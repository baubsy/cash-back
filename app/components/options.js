var React = require('react');
//component for selecting options such as, hide amounts, do a set?, timed?
//pass state to app?
var Options = React.createClass({
  render: function () {
    return <p>temp options</p>;
  }
});

module.exports = Options;
